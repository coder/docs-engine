/**
 * @coder/docs-config
 *
 * Typed configuration schema and shared types for the Coder docs engine.
 *
 * Placeholder scaffold: the configuration schema and the versions/content
 * provider types land in follow-up work. This module only establishes the
 * package boundary and toolchain for now.
 */

/** Package identifier, exported so the scaffold has a real, buildable symbol. */
export const DOCS_CONFIG_PACKAGE = "@coder/docs-config" as const;
