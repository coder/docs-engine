{
  "name": "@coder/docs-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Coder docs engine: the renderer, transforms, theme, and UI components behind Coder's documentation. Content lives in coder/coder.",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "biome lint .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
